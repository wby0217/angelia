import { autorun, observable, computed, action } from 'mobx';import { getPayGroup } from '../service';export default class PayGroupStore {    @observable payGroup: Object = {};    constructor(){        this.fetchPayGroup();    }    async fetchPayGroup () {        try {            const res = await getPayGroup();            this.payGroup = res.data;        } catch (err) {            this.payGroup = {};        }    }}