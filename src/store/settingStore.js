import { autorun, observable, computed, action } from 'mobx';export default class AppSettingStore {    @observable appSettings: Object = {};    constructor(){        this.restoreAppSettingsFromStorage();    }    restoreAppSettingsFromStorage() {        storage.load({key: 'appSettings'})            .then(res => {                this.appSettings = res;            })            .catch(err => {                console.log(err);            })    }}