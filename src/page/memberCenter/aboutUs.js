import React, { Component } from 'react';import WebPage from '../../component/webPage';import { View } from 'react-native';import Header from '../../component/header';export default class AboutUs extends Component {    static navigationOptions = {        header: null    };    constructor(props){        super(props);        this.state = {            source: {uri: ''}        };        this.navigation = props.navigation;    };    restoreAppSettingsFromStorage () {        storage.load({key: 'appSettings'}).then((res) => {            console.log(res);            this.setState({                source: {uri: res.about}            });        }).catch(err => {            console.log(err);        })    };    componentWillMount() {        this.restoreAppSettingsFromStorage()    };    render () {        const {source} = this.state;        if (!source.uri) {            return false        } else {            return (                <View style={{flex:1}}>                    <Header headerTitle = '关于我们'                            navigation = {this.navigation}/>                    <WebPage source={source} />                </View>            )        };    }};