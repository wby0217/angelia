import React, {    NetInfo} from 'react-native';const NOT_NETWORK = "网络异常，请稍后再试";const TAG_NETWORK_CHANGE = "网络变化";// const checkNetworkState = (callback) =>{//     NetInfo.fetch().done(//         (isConnected) => {//             callback(isConnected.toLowerCase());//         }//     );// };const checkNetworkState = (callback) =>{    NetInfo.getConnectionInfo().then(        ({type, effectiveType}) => {            callback(type.toLowerCase());        }    ).catch(err => {        console.log('== checkNetworkState error ===', err)    });};const removeEventListener = (tag,handler) => {    NetInfo.removeEventListener(tag, handler);};const addEventListener = (tag,handler)=>{    NetInfo.addEventListener(tag, handler);};export default{    checkNetworkState,    addEventListener,    removeEventListener,    NOT_NETWORK,    TAG_NETWORK_CHANGE}