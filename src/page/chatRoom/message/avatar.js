import React from "react";import {    Image,    StyleSheet,    View,    Dimensions,} from "react-native";const { width } = Dimensions.get('window');export default class Avatar extends React.Component {    render () {        const { avatar, position } = this.props;        const imageUrl = avatar ? {uri: avatar} : require('../../../assets/images/my_avatar.webp');        return (            <View style={styles[position].container} >                <Image style={styles.image} source={imageUrl}/>            </View>        )    }}const styles = {    image: {        width: 33,        height: 33,    },    left: StyleSheet.create({        container: {            flexDirection: 'row',            alignSelf: "flex-start",            marginRight: 8,        },    }),    right: StyleSheet.create({        container: {            marginLeft: 13,            flexDirection: 'row',            alignSelf: "flex-start",        }    }),};Avatar.defaultProps = {    position: 'left',}