import React, { Component } from 'react';import {    Keyboard,    View,} from 'react-native';import ScrollableTabView from 'react-native-scrollable-tab-view';import { withNavigationFocus } from 'react-navigation-is-focused-hoc';import DefaultTabBar from '../../../component/customTabBar';import ScanCodeToPay from './scanToPay';import SubmitPayResult from './submitPayResult';import Header from '../../../component/header';export default class FriendPay extends Component {    static navigationOptions = {        header: null    };    constructor(props) {        super(props);        this.params = props.navigation.state.params;        this.navigation = props.navigation;        this.headerTitle = this.navigation.state.params.rechargeInfo.name    }    render () {        return (            <View style={{flex:1}}>                <Header headerTitle = {this.headerTitle}                        navigation = {this.navigation}/>                <ScrollableTabView                    renderTabBar={() => <DefaultTabBar/>}                    tabBarUnderlineStyle={{ backgroundColor: '#FF0033',height: 2 }}                    tabBarActiveTextColor="#DD1B00"                    tabBarInactiveTextColor="#2D2D2D"                    tabBarBackgroundColor="#fff"                    onChangeTab = {Keyboard.dismiss}                    initialPage={0}>                    <ScanCodeToPay  tabLabel="扫码支付" params={this.params } />                    <SubmitPayResult tabLabel="已支付，我要提单" params={this.params } navigation={this.navigation}/>                </ScrollableTabView>            </View>        )    }}