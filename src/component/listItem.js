import React, { Component } from 'react';import {    View,    Text,    StyleSheet,    Image,    TouchableOpacity} from 'react-native';import { Icon } from './customicons'export default class ListItem extends Component {    constructor(props){        super(props);        this._onPress = this._onPress.bind(this);    }    _onPress () {        const { onPresshandle } = this.props;        if(onPresshandle) {            onPresshandle()        }    };    render () {        return (            <TouchableOpacity                style={[styles.listItem, this.props.style]}                onPress={() => this._onPress()}            >                {                    this.props.iconName ? (                        <View style={[styles.iconBox,{backgroundColor:this.props.bgColor}]}>                            <Icon name={this.props.iconName} size={15} color='#fff'/>                        </View>                    ) : null                }                <Text style={{fontSize: 15}}>{this.props.title}</Text>                <Text style={styles.remind}>{ this.props.remind}</Text>                <Icon style={styles.entryIcon} name='icon-right-arrow-normal' size={20} color='#ccc'/>            </TouchableOpacity>        )    }}const styles = StyleSheet.create({    listItem: {        height: 44,        backgroundColor: '#FFF',        alignItems: 'center',        flexDirection: 'row',        borderBottomWidth: StyleSheet.hairlineWidth,        borderBottomColor: '#E5E5E5',        paddingLeft: 10,    },    iconBox: {        flexDirection: 'row',        width: 23,        height: 23,        alignItems: 'center',        justifyContent: 'center',        borderRadius: 4,        marginRight: 12,    },    renderIcon: {        width: 23,        height: 23,        marginRight: 10,    },    entryIcon: {        width: 18,        position: 'absolute',        right: 12,        top: 12,    },    remind: {        position: 'absolute',        right: 30,        top: 15,        fontSize: 14,        color: '#999'    }})