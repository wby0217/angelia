import React, { Component } from 'react';import {    View,    Text,    StyleSheet,    TouchableOpacity} from 'react-native';export default class AppButton extends Component {    constructor(props) {        super(props)        this.state = {            isDisabled: true,        };        this._onPress = this._onPress.bind(this);    };    _onPress () {        const {isDisabled, onPressHandle} = this.props;        if(!isDisabled) {            onPressHandle && onPressHandle();        }    };    componentWillMount() {        const {styleDisabled, styleActivitied, isDisabled} = this.props;        this.resetStyleActivitied = styleActivitied ? styleActivitied : styles.activitied;        this.resetStyleDisabled = styleDisabled ? styleDisabled : '';    };    componentWillReceiveProps() {    };    render () {        return (            <TouchableOpacity                activeOpacity={this.props.isDisabled ? 1 : 0.2}                style={[                    styles.defaultButton,                    this.props.style,                    this.props.isDisabled ? this.resetStyleDisabled : this.resetStyleActivitied                ]}                onPress={() => this._onPress()}            >                <Text style={[                    {color: this.props.isDisabled ? '#B5B5B5' : '#FFF'}                ]}>                    {this.props.title}                </Text>            </TouchableOpacity>        )    }}const styles = StyleSheet.create({    defaultButton: {        flexDirection: 'row',        alignItems: 'center',        justifyContent: 'center',        paddingVertical: 10,        backgroundColor: '#E2E2E2',        borderRadius: 5,    },    btnText: {        fontSize: 18,    },    activitied: {        backgroundColor: '#FF0033',    }})