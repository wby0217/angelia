import React, { Component } from 'react';import {    StyleSheet,    ScrollView,    View,    Text,    TouchableOpacity,    Image,    Dimensions} from 'react-native';import HasContentView from './hasContentView';import HeXiaoView from './heXiaoView';import DefaultView from './defaultView';export default class SelectionAreaLHC extends Component {    constructor(props) {        super(props);        this.onSelectBallHandle = this.onSelectBallHandle.bind(this);    }    onSelectBallHandle (selected, selectedDisplay, prevMenu) {        const { onSelectBall } = this.props;        !!onSelectBall && onSelectBall(selected, selectedDisplay, prevMenu)    }    render () {        const { playInfo } = this.props;        const {meta, content} = playInfo;        if (meta === 'heXiao') {            return (                <HeXiaoView                    ref={ref => this.heXiaoView = ref}                    playInfo={playInfo}                    onSelectBall={this.onSelectBallHandle}/>            )        }        if (!!content) {            return (                <HasContentView                    ref={ref => this.hasContentView = ref}                    playInfo={playInfo}                    onSelectBall={this.onSelectBallHandle}/>            )        }        return (            <DefaultView                ref={ref => this.defaultView = ref}                playInfo={playInfo}                onSelectBall={this.onSelectBallHandle}/>        )    }}